# 결정 로그 (Decision Log)

원본 스펙(`docs/PRD-v5.md`)에 없거나, 구현하며 새로 내린 판단을 기록한다.
날짜·결정·이유·대안 순. 나중에 이 결정을 뒤집을 땐 여기에 추가로 남긴다.

---

## 2026-07-18 · 초기 구현

### 파싱 LLM: Groq `llama-3.3-70b-versatile` (무료)
- **변천**: `claude-opus-4-8`(최초) → opus 미접근 → `claude-sonnet-4-6` → Claude **크레딧 부족** → **Gemini 무료 티어** 시도했으나 유저 구글 계정이 기존 GCP 프로젝트에 묶여 **free tier `limit:0`**(새 프로젝트 키로도 동일) → **Groq 무료**로 최종 교체 (2026-07-20).
- **이유**: Groq는 Google Cloud 프로젝트/할당량 얽힘이 없어 카드 없이 확실히 무료. `console.groq.com/keys`에서 키 발급, Vercel 배포 가능, OpenAI 호환 API + `response_format: json_object`로 JSON 강제.
- **구현**: `lib/parse.ts`에서 REST(`api.groq.com/openai/v1/chat/completions`) 직접 호출. JSON 스키마는 시스템 프롬프트에 명시(json_object 모드). SDK 의존성 없음.
- **키**: `GROQ_API_KEY`.
- **트레이드오프**: Llama-3.3-70B는 Claude 대비 스텝 원자화·하이라이트 정밀도가 다소 낮을 수 있음. 무료 rate limit 존재(429 시 재시도).
- **재검토 트리거**: 품질 부족하면 Groq의 상위 모델 또는 (결제 되면) Claude로 복귀. 파싱이 `parseRecipeFromTranscript`로 격리돼 있어 `lib/parse.ts`만 교체하면 됨.
- **주의**: Gemini free tier `limit:0`은 "키가 기존 GCP 프로젝트에 붙어 무료 할당량이 없어서"가 원인. 이 계정에선 새 프로젝트로도 해결 안 됐음 → Gemini 재시도는 시간낭비.

### Tool Use는 `strict: false` + `tool_choice` 강제
- **이유**: `tool_choice: {type:"tool"}`만으로 안정적 JSON 확보. strict를 켜면 optional 필드(startTime 등)를 다루기 번거로움.
- **방어적 파싱**(`normalize`)으로 누락/이상값 흡수.

### 상태관리: Zustand 대신 경량 커스텀 store
- **이유**: localStorage 한 소스만 다루므로 `useSyncExternalStore` + 구독 패턴으로 충분. 의존성 최소화.
- **재검토 트리거**: 상태 공유가 복잡해지면 Zustand 도입.

### 폼: React Hook Form 미도입 (직접 작성/수정 폼은 useState)
- **이유**: 폼이 단순(동적 스텝 추가 정도). RHF 도입 이득 대비 오버헤드.

### 애니메이션: Framer Motion 미도입 (스와이프는 네이티브 터치/포인터 이벤트)
- **이유**: 의존성 줄이고 번들 가볍게. 스와이프는 pointer 이벤트로 충분.
- **재검토 트리거**: 전환 애니메이션 요구가 커지면 도입.

### PWA 서비스워커: next-pwa 대신 최소 커스텀 SW
- **이유**: next-pwa는 Next 15와 궁합 이슈가 잦음. 오프라인 앱쉘 캐싱 수준의 최소 SW로 충분(이번 스코프에 오프라인 요구 없음).

### 서버 캐시는 프로세스 인메모리 Map
- **이유**: 프로토타입. 서버리스에서 콜드스타트 시 사라지지만, 클라 localStorage 캐시가 주 방어선. 비용 절감 목적엔 충분.

### 텍스트 붙여넣기 파싱 경로 (자막 없는 영상 대안 + 줄글 레시피 입력) — 2026-07-20
- **문제**: 유튜브 자막이 없으면 `NoTranscriptError`로 저장 불가. 그러나 레시피 글은 설명란·고정 댓글·블로그·손메모에 이미 존재.
- **결정**: 자유 형식 텍스트를 LLM이 구조화하는 경로 추가. 자막 파서(`parseRecipeFromTranscript`)와 별도 프롬프트의 `parseRecipeFromText`를 두되 Groq 호출부(`callGroq`)·`normalize`는 공유.
- **API**: `POST /api/parse-recipe`에 `text` 모드 추가(`{ text, title?, url? }` → `{ ok, recipe, sourceType:"text" }`). URL 모드는 그대로. 예외 매핑은 `errorResponse`로 공통화.
- **저장**: `createRecipeFromText`는 `sourceType:"manual"`로 저장하되 태그 보존, `save` 로그 `metadata.sourceType:"text"`로 경로 구분(H-ACCESS 관찰용).
- **화면**: `/new`를 `붙여넣기(기본) | 직접 입력` 2모드로 재구성(`Suspense`+`useSearchParams`). 홈에서 자막 실패(`no_transcript`) 시 에러로 막지 않고 `/new?mode=paste&url=…` 붙여넣기 경로로 유도.
- **UX writing**: `저장`→**`담기`**(아카이브 담기 은유), AI 정리 액션은 **`레시피로 정리하기`**, 수정은 **`수정 완료`**로 통일. CategoryModal·클립보드 배너도 `담기`.
- **로딩**: `ParsingIndicator`(스피너+단계별 순차 문구) — URL/텍스트 각각 단계 문구. 버튼 내부에도 `Spinner` 노출.
- **스코프 확인**: PRD §14 스코프아웃(공유·비교·음성·AI Q&A 등)에 저촉 없음. 오히려 저장 진입 마찰 축소로 H-ACCESS 검증 표면을 넓힘.
- **트레이드오프**: 텍스트엔 타임스탬프가 없어 임베드 seek 불가 → `manual` 취급이 자연스러움. 순서 흐트러진 글은 LLM이 상식적으로 재배열(오파싱 여지 → 수정 화면으로 보정).

### 하단 탭바 도입 + 홈/아카이빙 분리 — 2026-07-20
- **결정**: 앱 최상위 내비를 하단 3탭으로 구성 — **홈**(레시피 변환+검색) · **레시피 추가**(`/new`) · **아카이빙**(`/archive`).
- **분리**: 기존 홈이 변환·검색·전체목록·필터를 모두 안고 있었음 → 전체 목록/카테고리·매체 필터·정렬은 `/archive`로 이관. 홈은 변환(URL/클립보드/자막없음 대안)과 검색(+최근 미리보기)만.
- **탭바 노출 범위**: 자체 하단 CTA가 있는 화면(`/new`, cook, edit)에는 탭바를 렌더하지 않고 홈·아카이빙에서만 노출(CTA 충돌 방지). `추가` 탭은 `/new`로 push되는 포커스 화면.
- **관찰 함의**: 홈 검색 vs 아카이빙 브라우징 어느 쪽이 재방문·재조회를 유발하는지 분리 관찰 가능(H-ACCESS/H-RETENTION). 두 목록 진입 모두 `view` 로그로 포착.

### 스텝 타이머 (레시피 시간 기반 프리셋 + 사용자 조정) — 2026-07-21
- **결정**: 쿠킹 모드 카드 뷰에서 시간이 필요한 스텝에 카운트다운 타이머를 노출. 레시피에 적힌 시간(스텝의 `time` 하이라이트 → 없으면 지시문 텍스트)에서 초를 추론해 기본값으로 프리셋하고, 사용자가 ±10초/±1분으로 조정 가능.
- **시간 파싱**(`lib/timers.ts::parseDurationToSeconds`): "5분"·"1분 30초"·"1시간"·"30초"·"약 10분" 등 합산 파싱. "2분의 1"(분수)은 `분(?!의)`로 제외. `stepTimerSeconds`는 LLM이 분류한 `time` 하이라이트를 우선 신뢰하고, 없을 때만 지시문 스캔(오탐 최소화).
- **세션 매니저**(`lib/timers.ts`): 타이머 상태를 세션 메모리(Map)에만 두고 localStorage 저장 안 함. 단일 인터벌(250ms)이 `endAt` 타임스탬프 기준으로 감산 → 스텝을 넘나들거나 컴포넌트가 언마운트돼도 상태 보존, 시간이 되면 한 번만 알림. storage.ts와 동일한 구독 패턴(`useSyncExternalStore`).
- **알림**: 인앱 한정 — Web Audio 삐 3회 + `navigator.vibrate`. 오디오는 시작 버튼(사용자 제스처) 시점에 활성화. **시스템 푸시 알림은 쓰지 않음** → PRD §14 스코프아웃의 "알림 트리거"(리텐션 유도 푸시)와 구분.
- **스코프 정합**: PRD의 "강제 금지: 자동 넘김·강제 대기 없음, 유저 페이스 존중"과 일치 — 타이머는 사용자가 직접 `시작`을 눌러야 하고, 완료돼도 스텝을 자동으로 넘기지 않는다(알림만).
- **배치**: 카드 뷰(`CookingModeCards`)에만 배치. 전체 뷰(`CookingModeOverview`)는 스텝이 `<button>` 내비 항목이라 중첩 인터랙션을 피하려 타이머 미배치 — 탭하면 해당 스텝 카드로 이동해 거기서 타이머 사용.

### 배포 환경 자막 추출 — Supadata(프록시 내장 서드파티 API) 경유 — 2026-07-21
- **문제**: `youtube-transcript`는 youtube.com을 직접 스크래핑하는데, YouTube가 데이터센터/클라우드 IP(Vercel 등)의 자막 요청을 봇으로 보고 차단 → 로컬(가정 IP)은 되지만 배포에선 `no_transcript`(422). 배포 API로 재현·확인함.
- **비해결 확인**: (1) 배포처 변경 → 모든 클라우드 IP 동일하게 차단되므로 무의미. (2) 공식 YouTube Data API `captions.download` → 채널 소유자 본인 영상만 가능, 남의 레시피 영상은 대상 아님. → 프록시 경유가 유일한 길.
- **결정**: 프록시를 내장한 서드파티 자막 API(**Supadata**, 무료 월 100건) 경유. `lib/transcript.ts`에 공급원 폴백 체인 구성. 키는 서버 환경변수 `SUPADATA_API_KEY`(클라 노출 금지, Groq 키와 동일 규칙).
- **크레딧 절약 분기**: `process.env.VERCEL`로 환경 판별. 로컬은 무료 직접 스크래핑 우선→Supadata 폴백, 클라우드는 Supadata 우선→스크래핑 최후 폴백. 키 없으면 스크래핑만(로컬 개발 무변경).
- **관찰 함의**: 자막 자동추출이 H-ACCESS(재방문 마찰) 검증에 실제로 중요한지 확인되기 전까지 최소 비용(무료 티어) 유지. 중요성 확인 시 유료 플랜/음성전사(Whisper) 승급 검토. 그전까지 자막 실패 대안은 붙여넣기 경로.

## 2026-07-23 · v6 P0 — 파싱 정확도 + 인분 정보 (상류 리스크 대응)

> 배경: v5 실사용(시현)·6인 시연에서 **파싱 정확도**가 프로덕트 최소 조건으로 확정되고, **인분·계량** 정보 부재가 4명 지적. 상류 리스크라 재실험 전 선행 필요. [[HYPOTHESES]] 상류 리스크, [[2026-07-22-interviews-v5-6people]]

### 파싱 프롬프트 재설계 (과분할·재료누락·오파싱·계량 정규화)
- **결정**: 자막용·텍스트용 두 프롬프트가 공유하는 `COMMON_RULES` 블록으로 리팩터링(규칙 드리프트 방지). 반영한 지적:
  - **과분할 방지**(제제): "넣는다/젓는다" 같은 연속 자잘한 동작을 억지 분리하지 말고 한 호흡 단위로 묶되, 서로 다른 조리 단계는 섞지 말 것.
  - **재료 누락 방지**(왓슨·이안·송송): 스텝에 등장하는 재료는 재료 목록에도 반드시 포함, 계량 없어도 재료 자체는 생략 금지.
  - **오파싱 교정**(시현 "굴소스→불소스"): 자막은 음성인식이라 오표기 가능 → 문맥상 명백한 표준 요리 용어로 교정, 확신 없으면 원문 유지.
  - **계량 정규화**: 큰술·작은술·컵·g·ml 등 표준 단위("한 스푼"→"1큰술").
- **검증**: 실제 Groq 호출 2건(김치볶음밥) — 재료 10~11개, 인분·계량 정상. 타임스탬프 매핑은 자막 품질에 의존(7/7 vs 0/10).

### 인분(`servings`) 스키마 + 인분 조정 UI — 벤지 요청
- **결정**: `ParsedRecipe`·`Recipe`에 `servings?: number`(원본 기준 인분) 추가. LLM이 추출(미상이면 생략, 추측 금지), `normalize`의 `parseServings`가 정수·"2~3인분" 문자열 모두 흡수(1~99 범위 밖은 버림).
- **UI**: 상세 화면 재료 위에 `ServingsIngredients`(인분 토글 1/2/3/4/6+기준) → `lib/servings.ts::scaleAmount`가 amount 문자열의 숫자 토큰(정수·소수·분수 `1/2`·범위 `2~3`)만 배수 적용, "약간" 등 비수치는 그대로. 기준 인분이 없으면 조정 UI 숨김. 편집 화면에 인분 입력 필드 추가(파싱 오류 보정).
- **트레이드오프**: 계량 스케일링은 자유 텍스트 파싱이라 완벽하지 않음(단위 혼용 등). 프로토타입 수준으로 충분, 정밀화는 니즈 확인 후.
- **2026-07-23 보정 추가(양념 sub-linear)**: 유저 지적 — "5인분이라고 라면 스프 5개 다 넣는 게 아니듯" 양념을 선형으로 늘리면 과함. → **주재료(isBasic=false)는 선형, 기본 양념(isBasic=true, 소금·간장·설탕 등)은 완만하게** 스케일(`seasoningFactor` = `factor^0.8`: ×2→1.74, ×5→3.62). 인분을 바꾼 경우 "양은 대략값, 양념은 완만히 조정했으니 간은 기호에 맞게 조절하세요" 안내 노출. **한계**: 재료별 정확한 보정은 요리 지식 필요라 근사; `isBasic`로 못 거르는 레시피 고유 양념(예: 라면스프=장볼재료)은 여전히 선형 — 안내로 보완.

### 2026-07-23 재설계 · 기준 인분(base)과 만들 양(target) 분리 + "모름"
- **문제 제기(유저)**: 영상 레시피가 1인분 기준이 아닐 수 있음(카레는 보통 4~5인분). LLM이 뽑은 인분을 **기준으로 그냥 신뢰하면 안 됨**. 사용자가 기준을 정해야 하고, 볼 때는 모를 수 있으니 **"모름"** 도 있어야 하며, 나중에 수정 가능해야 함.
- **결정**: 두 개념 분리.
  - **기준 인분(base) = `recipe.servings`**: 이 레시피 원본이 몇 인분이냐. LLM 추출값은 **제안(pre-fill)**일 뿐, `ServingsIngredients`의 `<select>`(1~6인분 / **모름**)로 사용자가 확정·수정 → `updateRecipe`로 저장. 편집 화면 인분 필드와도 일관.
  - **만들 양(target)**: 내가 몇 인분 만들거냐. 로컬 상태, 기준이 있을 때만 노출(버튼). 스케일 factor = target/base.
  - **기준 "모름"이면 스케일 안 함** — 원본 양 그대로 + "몇 인분 기준인지 정하면 양을 조절해 드려요" 안내. (근거 없는 배수를 강요하지 않음.)
- **폐기**: 이전엔 기준을 LLM값으로 고정하고 없으면 조정 UI 자체를 숨김 → "1인분 가정"·근거 없는 1~6 조정 문제. 이제 기준을 사용자 통제로.

## 2026-07-23 · 카테고리 UI 통합 (지정/미지정 혼동 제거)

- **문제(유저)**: 상세 화면 `CategoryPicker`에서 **지정된 카테고리(위, 주황 pill)**와 **기존 카테고리 제안(아래, `+칩` 아웃라인)**이 위치·스타일로 갈려 "둘이 다른 건가?" 혼동. 게다가 **✕의 의미가 이중**이었음(주황 pill의 ✕=이 레시피에서 해제, 아웃라인 칩의 ✕=전역 삭제).
- **결정**: 모든 카테고리를 **한 영역의 토글 칩**으로 통합. 지정됨=주황 채움 / 미지정=아웃라인(스타일로만 상태 구분). **칩 본체 탭=지정/해제 토글**, **✕=전역 완전 삭제(모든 칩에서 의미 동일)**. 새 카테고리는 입력→같은 칩 영역에 지정 상태로 합류. 순서는 기존 목록 기준 고정(토글 시 칩 이동 없음). `/new` ManualForm의 토글 칩 패턴과도 일관.

### 파싱 소스 확장 — 영상 설명란 병합 (댓글은 보류) — 왓슨 "자막<설명란/댓글 퀄리티"
- **결정**: 자막에 더해 **영상 설명란**을 2차 소스로 병합. `lib/transcript.ts::fetchVideoDescription`가 Supadata 메타 엔드포인트(`GET /v1/youtube/video?id=`, `description` 필드) 사용 — **기존 `SUPADATA_API_KEY` 재사용, 새 의존 없음**. 파싱 라우트에서 자막·oEmbed·설명란 3자 병렬 fetch, 프롬프트에서 "설명란을 자막보다 우선 신뢰하되 프로모션·링크 무시".
- **잡음·토큰 보호**: 설명란 2000자 캡(TPM 12k 한도 방어, 자막이 주 소스). 실패 시 빈 문자열로 자막만 파싱 계속.
- **댓글 보류 이유**: Supadata는 댓글 **본문** 미제공(개수만) → 자동 추출엔 YouTube Data API v3 신규 키·할당량 필요. 유저 결정으로 **이번엔 설명란만**, 댓글은 다음 라운드 재검토. (댓글 니즈는 붙여넣기 경로로 임시 흡수 가능)
- **검증**: 설명란 1646자 병합 시 재료 계량이 `김치 1컵(160g)`처럼 정밀해짐(설명란에서 보강 확인).

## 2026-07-23 · v6 P1-A — 카드-타임라인 연동 안정화 (H-EMBED 킬러 기능)

> 배경: 6인 중 4명이 카드-타임라인 연동을 "큰 차별점"으로 지목했으나 "될 때도 안 될 때도" = 안정성이 검증 관문. P0 검증 중 타임스탬프 매핑 편차(영상별 7/7~0/10)도 발견. [[HYPOTHESES]] H-EMBED, [[2026-07-22-interviews-v5-6people]]

### 영상 플레이어를 cook 페이지로 끌어올려 단일 인스턴스 공유 (A-3)
- **문제**: 카드 뷰·전체 뷰가 **각자** `YouTubeEmbed`를 마운트 → 뷰 토글 시 플레이어가 파괴·재생성되며 재생이 끊김(왓슨 지적).
- **결정**: `app/recipe/[id]/cook/page.tsx`가 플레이어를 소유(단 하나)하고 헤더 아래에 고정 렌더. `CookingModeCards`·`CookingModeOverview`는 임베드를 렌더하지 않고, 카드뷰는 `embedRef.seekTo`로 제어, 전체뷰는 부모가 재생 시각으로 계산한 `activeIndex`를 prop으로 받아 하이라이트. → 뷰를 오가도 동일 플레이어라 재생 유지.
- **트레이드오프**: 레이아웃상 영상이 항상 상단 고정(두 뷰 공통) — 기존 전체뷰의 sticky 임베드와 동일 결과. 뷰별 임베드 위치 커스텀은 포기(대신 안정성).

### 타임스탬프 폴백 — 일부 스텝만 시간정보가 있어도 연동 유지 (A-1)
- **문제**: 자막 품질에 따라 스텝별 `startTime`이 들쭉날쭉(0/10~7/7). 없는 스텝은 카드를 넘겨도 영상이 안 움직여 "연동이 안 된다"는 인상.
- **결정**: `effectiveStartTime(steps, index)` — 해당 스텝에 타임스탬프가 없으면 **바로 앞** 타임스탬프로, 그것도 없으면 뒤에서 채운다. 영상에 타임스탬프가 하나도 없을 때만 연동 미동작(seek 생략).
- **한계**: 근본 개선은 파싱 단계의 타임스탬프 매핑률↑. 폴백은 "끊긴 것처럼 보이는" 마찰을 줄이는 완화책. 매핑률 자체는 다음 파싱 개선 라운드 과제.

### 다음 카드로 넘길 때 영상 자동 정지 (A-2 — 송송)
- **문제**: 카드 이동 시 해당 구간을 **자동 재생**했음. 송송: "다음 단계 준비 시간이 필요하니 넘기면 멈춰 있으면."
- **결정**: 카드 스텝 이동 시 `seekTo(t, false)` = 구간으로 이동하되 **정지**(`YouTubeEmbedHandle`에 `pause()` 추가, `seekTo`의 play=false는 pause까지 수행). 유저가 직접 재생. 전체 뷰에선 자유 재생(자동 하이라이트)이라 이 강제 이동/정지를 걸지 않음.
- **스코프 정합**: PRD "자동 넘김·강제 없음, 유저 페이스 존중"과 일치.
- **검증**: typecheck 통과, `/cook`·`/cook?view=overview` 200. 플레이어 지속·seek·정지의 실제 동작은 실모바일 브라우저 손검증 필요(미확인).

## 2026-07-23 · v6 P1-B — UX 버그 배치 (신뢰도 마찰 제거)

> 6인 시연에서 나온 자잘한 혼란·버그. 개별 비용은 낮으나 쌓이면 "덜 만들어진 앱" 인상. [[2026-07-22-interviews-v5-6people]] "UX 세부 버그·혼란"

- **B-1 탭바 지속성 (벤지·이안 "탭바 사라짐")**: **cook만 몰입 화면(탭바 없음, 자체 ✕ 닫기) 유지**, `/new`·`edit`에는 `TabBar` 노출. 두 화면의 하단 고정 CTA는 탭바(높이 60px+세이프에어리어) 위로 올림 — 인라인 `style={{bottom:"calc(60px + env(safe-area-inset-bottom))"}}` (Tailwind JIT 스캔 의존 회피, 확실성). 본문 `pb-44`로 겹침 방지. **결정 근거**: 요리 몰입은 cook에 국한, 나머지는 IC 일관성이 더 중요(유저 선택).
- **B-2 붙여넣기 오류 (송송)**: 현재 코드는 붙여넣기 모드에서 **내용만 필수**·제목/URL 선택이라 이미 해소 상태(2026-07-20 텍스트 경로 작업). 코드 변경 없음, 확인만.
- **B-3 저장 피드백 부재 (벤지)**: 생성 3경로(홈 URL·붙여넣기·직접입력)가 저장 후 `/recipe/[id]?saved=1`로 이동, 상세 페이지가 `saved=1`이면 "레시피를 담았어요 ✓" 토스트 2.6초. `useSearchParams`의 Suspense 요구를 피해 `location.search`를 클라에서 직접 읽음.
- **B-4 아이콘 노션 토글처럼 보임 (송송)**: 맨 `▶`(빨간 삼각형)을 `YouTubeGlyph`(둥근 빨간 사각형+흰 삼각형, 보편 인식 유튜브 표식)로 교체 — SourceAttribution·RecipeCard 썸네일 배지·홈 담기존 헤더 3곳.
- **B-5 기본 양념 접힘 디폴트 (이안·송송)**: `IngredientChecklist`의 `basicOpen` 기본값 `false`→`true`(펼침 디폴트).
- **B-6 첫인상 "유튜브 링크만 되나?" (왓슨)**: 홈 담기존에 "링크가 없어요? 설명란·블로그 글을 붙여넣어 담기 →"(→ `/new?mode=paste`) 안내 링크 추가.
- **검증**: typecheck 통과, 변경 라우트 전부 200, SSR HTML에 각 변경 마커 확인. 레이아웃(탭바-CTA 겹침 여부)·토스트 타이밍은 실기기 손검증 권장.

## 2026-07-23 · v6 P1-A 후속 — 타임스탬프 매핑률 개선 (H-EMBED 근본 품질)

> P1-A 검증 중 발견: 스텝 `startTime` 매핑이 영상별 0/10~7/7로 편차. 설명란이 풍부한 영상에서 LLM이 설명란 기반으로 스텝을 만들며 자막 타임스탬프를 누락하는 패턴. 연동(H-EMBED)의 근본 품질 문제. [[HYPOTHESES]] H-EMBED

### 프롬프트 강화 + 결정론적 후처리 폴백
- **프롬프트**: 자막용 시스템 프롬프트에 "스텝 startTime은 **반드시 자막 [분:초]에서**, 설명란은 타이밍 근거 아님, 모든 스텝에 부여"를 명시.
- **후처리**(`lib/timestamps.ts::assignStepTimestamps`): LLM이 놓친 스텝을 원본 자막 세그먼트와 **토큰 겹침 매칭**으로 복구. 한글 2음절+/영숫자 토큰화, 하이라이트(재료 등) 토큰 가중 2, **비감소(monotonic) 포인터**로 뒤로 점프 방지. 겹침 0이면 비워두고 렌더 시 `effectiveStartTime`이 앞 값 이어받음. `parseRecipeFromTranscript`가 `segments`를 받아 `normalize` 후 적용.
- **검증(결정론)**: 실제 자막(183세그먼트) 기반, 스텝 startTime을 전부 제거한 0/N 상황에서 **61/61 복구, 비감소 유지, ±10초 정확도 58/61(95%)**. LLM end-to-end 실측은 Groq 무료 일일 토큰 소진으로 보류(리셋 후 측정).
- **트레이드오프**: 실제 LLM 스텝은 의역돼 토큰 겹침이 낮을 수 있음(테스트는 낙관적). 하이라이트 정확매칭 + 렌더 폴백이 안전망. 복구율 자체는 다양한 영상으로 추가 측정 필요.

## 2026-07-23 · v6 P2 — 다음 스텝 한 줄 미리보기 (H-VIEW 별도 검증 표면)

> 왓슨: "요리하며 다음 단계가 궁금 → 한 카드에 다음 스텝 축약을 같이." 그러나 목록(오버뷰)은 "너무 요약돼 안 봄"으로 거부. → **오버뷰가 아니라 '다음 하나'만** 축약 노출. [[HYPOTHESES]] H-VIEW, [[2026-07-22-interviews-v5-6people]]

- **결정(전문 상시 노출)**: 쿠킹 카드 뷰(`CookingModeCards`) 하단, 진행바 위에 다음 스텝 **지시문 전문(`text`)을 작은 글씨·낮은 대비(text-sm, white/45)로 상시 노출**. 현재 스텝(text-3xl)을 방해하지 않는 선. 상단 구분선 + "다음" 라벨. 마지막 스텝에선 미표시. 별도 탭/인터랙션 없음(읽기 전용).
- **"너무 요약" 회피**: `summary`(오버뷰용 1줄 요약)를 버리고 **전문을 그대로** 보여줌 — 왓슨이 지적한 "너무 요약돼 안 봄"을 근본 회피. 시각적 위계(작게·흐리게)로 방해 최소화.
- **검토했다 버린 대안(peek)**: 평소 요약 한 줄 + 탭하면 전문 펼침(peek)안 → 요리 중 탭 동작(젖은 손) 부담 + 요약이 여전히 방해. **전문 상시노출이 더 단순·유효**로 판단해 채택.
- **거부된 오버뷰와 구분**: 전체 목록이 아니라 **다음 1개만**. "카드 한 장=한 동작" 원칙 유지(미리보기는 읽기용, 조작 단위는 현재 카드).
- **관찰 함의**: H-VIEW의 재해석("유저가 원하는 건 전체 오버뷰가 아니라 카드 내 다음 스텝 미리보기") 검증 표면. 다음 시연에서 방해도·유용성 관찰.

## 2026-07-23 · v6 쿠킹 모드 버그·정리 3건

- **버그: 미재생 상태에서 카드 넘기면 영상 검게·클릭 먹통.** 원인: 스텝 이동 시 `seekTo(t,false)`가 seek+pause를 하는데, **한 번도 재생 안 한(cued/unstarted) 플레이어**에 seekTo는 자동재생을 유발하고 곧바로 pause하면 검은 프레임에서 멈춰 클릭이 안 먹음. 해결: `YouTubeEmbed`가 `onStateChange`로 재생 시작 여부(`startedRef`)를 추적 → 스텝 이동 시 **이미 재생한 경우만 seek+pause**, 아직 안 한 경우는 `cueVideoById`로 **재큐잉**(썸네일 유지·클릭 가능·재생 시 올바른 스텝부터).
- **임베드 불가 영상: 안내 문구 대신 영상 영역 자체 제거.** 기존 "임베드가 제한되어 있어요 + 유튜브에서 보기" 폴백을 없애고, `YouTubeEmbed`는 차단 시 `null` 반환 + `onBlocked` 호출. cook 페이지는 `embedBlocked` 상태로 **영상 컨테이너 자체를 렌더 안 함**(검은 바도 안 남김). (PRD §12의 임베드 폴백 방침 변경 — 유저 요청.)
- **다음 스텝 미리보기 레이아웃 정렬.** "다음" 라벨과 지시문을 `items-baseline`으로 정렬, 라벨 크기를 본문과 맞춰(text-sm) 시각 정돈.

## 2026-07-23 · v6 요리 중 오타 즉시 수정 (인라인 편집) — 파싱 오류 상시 완화책

> 파싱 정확도를 높여도 오타·오인식은 계속 남는다(상류 리스크). 별도 편집 화면까지 안 가고 **요리하다 눈에 띈 오타를 그 자리에서** 고치게 함. [[HYPOTHESES]] 상류 리스크

- **결정**: 쿠킹 카드 뷰(`CookingModeCards`) **상단 메타 행 오른쪽 구석에 "✏️ 수정하기"**(text-xs·white/30로 은은, 본문 시선 방해 최소화) → 탭하면 지시문이 **인라인 textarea**로 바뀌고 취소/저장. 저장 시 부모(cook)의 `editStepText(i, text)`가 해당 스텝 `text`만 갱신해 `updateRecipe`로 localStorage 저장 → store 반영 즉시 카드 갱신. (초기엔 본문 아래 중앙 배치였으나 시선 강탈이라 상단 구석으로 이동, 라이팅도 '고치기'→'수정하기'.)
- **간편성 우선**: 텍스트만 수정(재료·순서·타임스탬프·하이라이트는 편집 화면에서). 편집 중엔 **스와이프 비활성화**, 스텝을 넘기면 편집 자동 취소(오작동 방지). 빈 값/무변경이면 저장 안 함.
- **하이라이트**: 편집 후 기존 하이라이트 값이 새 텍스트에 없으면 자동으로 표시만 사라짐(오류 아님) — 재매핑은 스코프 밖.
- **배치 범위**: 카드 뷰만. 전체 뷰는 스텝이 내비 버튼이라 인라인 편집 미배치(탭하면 카드로 이동해 거기서 수정).

## 2026-07-23 · 배포 파싱 422(no_transcript) 회귀 수정 — Supadata 동시 호출 제거

- **증상**: 배포에서 `/api/parse-recipe`가 갑자기 `422 no_transcript`. 그러나 `/api/youtube-transcript`(자막만)는 200 정상.
- **원인**: P0에서 추가한 `fetchVideoDescription`(Supadata **metadata**)가 파싱 라우트에서 `fetchTranscript`(Supadata **transcript**)와 **Promise.all로 동시 호출**됨. Supadata **무료 티어는 동시요청 제한**이라 둘 중 하나가 `429 limit-exceeded` → 자막 쪽이 걸리면 스크래핑 폴백(클라우드 IP 차단)으로 넘어가 `NoTranscriptError`(422). 로컬은 자막을 스크래핑으로 먼저 받아 Supadata 경합이 없어 안 드러남(직접 재현: 동시호출 시 transcript 429 / metadata 200 확인).
- **수정**: 자막(Supadata)+oEmbed(비-Supadata)만 병렬, **설명란은 자막 성공 후 순차** 호출. 이로써 Supadata 동시 호출이 사라짐. 설명란 실패(429 등)는 `""` 반환으로 무해(자막만으로 파싱). `scripts/test-parse.ts`도 순차로 통일.
- **교훈**: 무료 티어 외부 API는 **동시성**도 쿼터만큼 제약. 같은 공급원 호출은 직렬화. 설명란은 파싱당 Supadata 1건을 추가로 쓰므로 월 100건 한도 소진이 2배 빨라지는 점 유의(대량 사용 시 재검토).
- **검증**: 로컬 재파싱 정상 — 인분 "1인분" 추출, 설명란 병합 계량 정밀, **타임스탬프 10/10**(P0 때 0/10이던 영상). 배포 반영은 push 후 확인 필요.

## 참고 · "인분 기능이 안 보인다"의 실제 원인
- 인분(`servings`) 추출·조정 UI는 정상 동작(위 로컬 검증에서 "1인분" 확인). 안 보였던 이유는 **위 422로 새 레시피 파싱 자체가 실패**해 신규 저장이 안 됐고, **기존 레시피는 servings 도입 이전 것**이라 값이 없어 조정 UI가 숨겨진 것. 422 수정 배포 후 **새로 담은 레시피**에서 인분 기준·조정이 나타남(단 LLM이 인분을 못 찾은 레시피는 미표시 — 설계상 정상).
- **item 4(홈 정보구조 재설계)는 보류**: 카테고리 상단·아카이빙 통합은 H-RETENTION 재포지셔닝과 얽혀 가설이 흔들리는 영역 → 260723_v6 시연에서 방향 확인 후 착수(지금 선구현은 재뒤집힘 리스크).
